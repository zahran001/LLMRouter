[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: requires the live mock server (Tier 2)
    eval: the deterministic eval gate (Tier 3)
    negative_control: proves the eval suite can fail (Tier 3)
    slow: takes more than a couple seconds
