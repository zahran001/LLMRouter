{
  "what": "Repeat/evidence policy for GPU session #2 (R4 README R10/\u00a711).",
  "status": "PROPOSED -- requires human sign-off at Hard Stop R-PREGPU. metrics.classification.RepeatPolicy consumes this file rather than hard-coding values, so the numbers stay a recorded human decision.",
  "proposed_at": "2026-08-19",
  "policy": {
    "min_valid_repeats": 3,
    "require_unanimous": true,
    "n_per_run": 4000,
    "n_max": 5000,
    "max_repeats_authorized": 3,
    "max_ttft_p99_spread_ms": null
  },
  "rationale": {
    "min_valid_repeats": "Three is the smallest number that can show disagreement AS disagreement: with two, a split is a tie and there is no way to tell which run was unusual.",
    "require_unanimous": "Near the SLO a 2-1 split IS the finding -- the point is unstable. Taking the majority would convert an honest UNCERTAIN into a verdict, which is the failure the first session already made once.",
    "n_per_run": "Smallest grid candidate reaching a <=5% per-run classification-flip rate on the conservative 2-RPS bootstrap source (R3 evidence package 4.1).",
    "n_max": "Structural: the pinned corpus holds 5,000 prompts, so a larger canonical multiset would require repeating prompts, which is not neutral with prefix caching and is forbidden.",
    "max_repeats_authorized": "Three at N=4,000 is ~5.4h of drive time in the worst-case lambda set. Beyond that the session length itself becomes the dominant risk (spot preemption), not the statistics.",
    "max_ttft_p99_spread_ms": "Left null deliberately. Spread is RECORDED for every point; gating on a threshold nobody has calibrated would invent a rule, and unanimity already catches the case that matters."
  },
  "escalation": {
    "when_permitted": "At most ONE escalation, and only if exactly one boundary-determining lambda remains UNCERTAIN after 3 valid repeats at N=4,000.",
    "what_it_is": "Re-drive that single lambda at N=5,000 for all 3 repeats. Nothing else changes: same canonical membership (the full 5,000-prompt corpus at N_max), same seeds scheme, same server.",
    "cost_at_lambda_2": "3 x 5000/2 = 7,500s = 2.1h additional drive time.",
    "requires": "Explicit human authorization BEFORE the session, recorded here. Without it, an UNCERTAIN boundary point is reported as an interval.",
    "authorized": null
  },
  "stop_condition": {
    "rule": "If the crossing is still unresolved once the authorized ceiling is reached, report breach interval = (highest defensible UNDER lambda, lowest defensible OVER lambda] and stop.",
    "never": [
      "escalate N beyond 5,000",
      "repeat corpus prompts to manufacture a larger N",
      "add unplanned repeats on the meter",
      "take a majority verdict from split repeats"
    ]
  }
}