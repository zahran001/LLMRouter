# Python dependencies for metrics/, mock/, and their test suites.
# Install into a venv: python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

numpy>=2.0
httpx>=0.28
starlette>=0.40
uvicorn>=0.30

# test tooling
pytest>=8.0
pytest-asyncio>=0.24
