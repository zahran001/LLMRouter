#!/usr/bin/env bash
#
# reproduce.sh
#
# Intent: one-command end-to-end reproduction of benchmark results, from a
# clean environment through to the CSVs/charts in benchmarks/. Eventually
# this will provision infra, start the router and mock replicas, run the
# load generators, and compute metrics.

echo "TODO: implement reproduce"
